# Pinned dependency lockfile.
# Direct dependencies: fastapi, uvicorn, yt-dlp, python-dotenv, requests
# (requests isn't imported by the app; yt-dlp prefers it as its HTTP backend when present)
#
# To update: in a FRESH venv run
#   pip install fastapi uvicorn yt-dlp python-dotenv requests
#   pip freeze > requirements.txt
# then re-add this header. A fresh venv keeps dev tools (flake8 etc.) out of the
# freeze; setuptools/wheel are additionally pinned in the Dockerfile.

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.5.20
charset-normalizer==3.4.7
click==8.4.1
fastapi==0.136.3
h11==0.16.0
idna==3.18
packaging==26.2
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
requests==2.34.2
setuptools==82.0.1
starlette==1.3.0
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
uvicorn==0.49.0
wheel==0.47.0
yt-dlp==2026.6.9
